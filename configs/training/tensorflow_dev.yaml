training:
  backend: tensorflow
  seed: 42
  batch_size: 8
  epochs: 3
  learning_rate: 0.0003
  runtime: cuda
  experiment_name: tensorflow_dev
  checkpoint_name: latest.keras
  max_samples: 128
  augmentation:
    preset: standard
