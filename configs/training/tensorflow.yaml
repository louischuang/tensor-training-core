training:
  backend: tensorflow
  seed: 42
  batch_size: 8
  epochs: 12
  learning_rate: 0.0003
  runtime: cuda
  experiment_name: tensorflow_baseline
  checkpoint_name: latest.keras
  augmentation:
    preset: standard
