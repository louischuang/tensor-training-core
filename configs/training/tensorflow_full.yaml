training:
  backend: tensorflow
  seed: 42
  batch_size: 16
  epochs: 4
  learning_rate: 0.0002
  runtime: cuda
  experiment_name: tensorflow_full
  checkpoint_name: latest.keras
  augmentation:
    preset: aggressive
